import React,{useContext} from "react";
import ThemeContext from "./ThemeContext";

const MainContent = () => {
    return (
        <div>
            
        </div>
    );
};

export default MainContent;
